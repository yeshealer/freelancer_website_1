import ReactDOM from 'react-dom';
import React from 'react';

const onLineLeaveCountry = () => {
  ReactDOM.render(<div />, document.getElementById('tooltip-root'));
};

export default onLineLeaveCountry;
